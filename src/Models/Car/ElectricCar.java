package Models.Car;

public abstract class ElectricCar extends Car {
    public ElectricCar(String carId,String model, String build, String color) {
        super(carId, model, build, color);
    }
}
